#include <iostream>

using namespace::std;

int main() {
    int a, b;
    cout << "Valor inicial e Valor Final:" << endl;
    cin >> a >> b;
    for(a; a<=b; a++ ) {
        cout << a*a << endl;
    }

return 0;
}